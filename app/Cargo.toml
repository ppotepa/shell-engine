[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
engine = { path = "../engine" }
engine-mod = { path = "../engine-mod" }
clap = { workspace = true }
serde_yaml = { workspace = true }
