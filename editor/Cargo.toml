[package]
name = "editor"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
engine-core = { path = "../engine-core" }
engine = { path = "../engine" }
engine-events = { path = "../engine-events" }

[dev-dependencies]
tempfile = "3.13"
