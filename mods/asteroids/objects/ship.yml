kind: object
name: ship
# Visible ship mesh now lives in game/layers/ship-scene-layer.yml so it shares
# the same stable scene-camera path as asteroids and no longer rides the prefab
# object layer.
sprites: []
