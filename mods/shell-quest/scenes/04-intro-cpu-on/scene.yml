# yaml-language-server: $schema=../../schemas/scenes.yaml
id: intro-cpu-on
title: Intro CPU On
cutscene: true
bg: black
layers:
  - ref: main
audio:
  on_enter:
    - { at_ms: 4600, cue: beep, volume: 1.0 }
stages:
  on_enter:
    steps:
      - { pause: 13000ms }
  on_idle:
    trigger: timeout
    steps: [{ pause: 1ms }]
  on_leave:
    steps: [{ pause: 80ms }]
next: mainmenu
