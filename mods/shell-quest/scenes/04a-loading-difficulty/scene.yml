# yaml-language-server: $schema=../../schemas/scenes.yaml
id: 04a.loading.difficulty
title: Loading Difficulty
bg: black
# Prerender all OBJ sprites from the difficulty-select scene into ObjFrameCache.
# The engine bakes them in a background thread then auto-transitions to `next`.
prerender:
  - 04.intro.difficulty-select
next: 04.intro.difficulty-select
stages:
  on_enter:
    steps:
      - pause: 1ms
layers: []
