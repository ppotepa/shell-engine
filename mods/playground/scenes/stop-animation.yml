# yaml-language-server: $schema=../schemas/scenes.yaml
# Authored YAML v2 example: prefer `bg`, `fg`, `at`, and `pause` shorthands where the current compiler supports them.
id: playground-stop-animation
title: Stop Animation (Placeholder)
bg: black
stages:
  on_enter:
    steps:
      - { effects: [{ name: fade-in, duration: 300 }] }
  on_idle:
    trigger: any-key
    steps: [{ pause: 1ms }]
  on_leave:
    steps:
      - { effects: [{ name: fade-out, duration: 250 }] }
layers:
  - { name: placeholder, z_index: 0, sprites: [{ type: text, content: "PLACEHOLDER TEXT", at: cc, y: 0, font: "generic:1", fg: white }] }
next: playground-menu
