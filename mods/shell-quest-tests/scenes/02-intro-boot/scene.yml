# Test variant — compressed for benchmarking
# Original: ~12290ms, compressed: ~2200ms
id: 02.intro.boot
title: 02 Intro Boot (Test)
cutscene: true
bg: black
layers:
  - ref: main
stages:
  on_enter:
    steps:
      - { duration: 120, effects: [{ name: fade-in, duration: 120, params: { easing: ease-out } }] }
      - { duration: 1600, effects: [{ name: scanlines, duration: 1600 }] }
      - { duration: 200, effects: [{ name: fade-out, duration: 200, params: { easing: ease-in } }] }
  on_idle:
    trigger: timeout
    timeout: 200ms
    steps: [{ pause: 1ms }]
  on_leave:
    steps: [{ pause: 60ms }]
next: 03.intro.lab-enter
