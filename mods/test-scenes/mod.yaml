# yaml-language-server: $schema=./schemas/mod.yaml
name: test-scenes
version: 0.1.0
entrypoint: /scenes/menu/scene.yml
terminal:
  min_colours: 256
  # Compatibility exception: these comparison scenes intentionally exercise
  # output-sized presentation instead of the fixed authored-canvas default.
  render_size: match-output
  presentation_policy: stretch
  min_width: 320
  min_height: 240
