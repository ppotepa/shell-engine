$schema: https://json-schema.org/draft/2020-12/schema
$id: https://shell-quest.local/schemas/generated/shell-quest.scene-file.schema.yaml
title: shell-quest scene-file overlay schema
allOf:
- $ref: ../scene-file.schema.yaml
- properties:
    next:
      oneOf:
      - $ref: ./shell-quest.schema.yaml#/$defs/scene_ids
      - type: 'null'
    menu-options:
      items:
        properties:
          next:
            $ref: ./shell-quest.schema.yaml#/$defs/scene_ids
    menu_options:
      items:
        properties:
          next:
            $ref: ./shell-quest.schema.yaml#/$defs/scene_ids
    objects:
      items:
        properties:
          use:
            $ref: ./shell-quest.schema.yaml#/$defs/object_names
