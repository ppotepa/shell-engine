[package]
name = "engine-capture"
version = "0.1.0"
edition = "2021"

[dependencies]
engine-core = { workspace = true }
engine-error = { path = "../engine-error" }

[dev-dependencies]
tempfile = "3.13"
